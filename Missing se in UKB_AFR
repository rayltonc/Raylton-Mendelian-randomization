library(data.table)

ukb <- fread("HbA1c_UKB_for_METAL.txt")

ukb_clean <- ukb[
  !is.na(SE) &
  is.finite(SE) &
  SE > 0
]

nrow(ukb)
nrow(ukb_clean)
nrow(ukb) - nrow(ukb_clean)

fwrite(
  ukb_clean,
  file = "HbA1c_UKB_for_METAL.clean.txt",
  sep = "\t",
  quote = FALSE
)
